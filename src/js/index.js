import store from "../js/store/index"; // dev
import { addPass, changeNumber, newClick } from "../js/actions/index";

window.store = store;
window.addPass = addPass;
window.changeNumber = changeNumber;
window.newClick = newClick;